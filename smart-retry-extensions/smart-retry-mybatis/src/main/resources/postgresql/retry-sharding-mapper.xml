<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.retry.mybatis.dao.RetryShardingDao">

	<resultMap id="RetryTaskMAP" type="com.smart.retry.mybatis.entity.RetryShardingDO">
		<result column="id" property="id"/>
		<result column="gmt_create" property="gmtCreate"/>
		<result column="creator_id" property="creatorId"/>
		<result column="instance_id" property="instanceId"/>
		<result column="status" property="status"/>
		<result column="status" property="status"/>
		<result column="last_heartbeat" property="lastHeartbeat"/>
	</resultMap>

	<sql id="all_column">
		id,
		gmt_create,
		creator_id,
		instance_id,
		status,
		last_heartbeat
	</sql>

	<insert id="insert">
		INSERT INTO retry_sharding (
			gmt_create,
		    creator_id,
			instance_id,
			status,
			last_heartbeat)
		VALUES (now(),
		        #{creatorId},
				#{instanceId},
				#{status},
				#{lastHeartbeat})
	</insert>

	<select id="selectByInstanceId"
			parameterType="java.lang.String"
			resultMap="RetryTaskMAP">
		SELECT <include refid="all_column"/>
		    FROM retry_sharding
		WHERE instance_id = #{instanceId}
	</select>

    <!-- 管理端分页查询分片，按创建者和归属实例模糊过滤 -->
    <select id="selectAllWithPage" resultMap="RetryTaskMAP">
        SELECT <include refid="all_column"/>
        FROM retry_sharding
        <where>
            <if test="creatorId != null and creatorId != ''">
                AND creator_id LIKE CONCAT('%', #{creatorId}, '%')
            </if>
            <if test="instanceId != null and instanceId != ''">
                AND instance_id LIKE CONCAT('%', #{instanceId}, '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 管理端统计分片总数，条件与分页查询保持一致 -->
    <select id="countAll" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM retry_sharding
        <where>
            <if test="creatorId != null and creatorId != ''">
                AND creator_id LIKE CONCAT('%', #{creatorId}, '%')
            </if>
            <if test="instanceId != null and instanceId != ''">
                AND instance_id LIKE CONCAT('%', #{instanceId}, '%')
            </if>
        </where>
    </select>

    <!-- 按主键查询分片 -->
    <select id="selectById" parameterType="java.lang.Long" resultMap="RetryTaskMAP">
        SELECT <include refid="all_column"/>
        FROM retry_sharding
        WHERE id = #{id}
    </select>



    <!-- 兼容 PostgreSQL -->
    <update id="scrambleDeadSharding"  parameterType="map">
        WITH expired AS (
        SELECT id
        FROM retry_sharding
        WHERE last_heartbeat &lt; NOW() - (#{timeout}::text || ' seconds')::interval
        ORDER BY id
        LIMIT 1
        )
        UPDATE retry_sharding
        SET
        status = #{status},
        instance_id = #{instanceId},
        last_heartbeat = NOW()
        FROM expired
        WHERE retry_sharding.id = expired.id
        <![CDATA[
        AND retry_sharding.last_heartbeat < NOW() - (#{timeout}::text || ' seconds')::interval
        ]]>
    </update>


	<update id="updateLastHeartbeat">
		UPDATE retry_sharding
		SET
		last_heartbeat = now(),
		status = #{status}
		WHERE
		instance_id = #{instanceId}
	</update>


</mapper>
